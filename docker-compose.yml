version: "3.9"

services:
    app:
        build: .
        ports:
            - 80:8080
        volumes:
            - storage:/etc/storage
        networks:
            - discord-bot

volumes:
    storage:

networks:
    discord-bot:
        external: true
